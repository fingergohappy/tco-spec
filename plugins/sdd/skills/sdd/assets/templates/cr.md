---
id: {{ID}}
kind: change                       # change = 改已有 REQ 的行为 (本模板); charter = 全新业务立项 (cr-new.md)
status: to fix                     # to fix | fixing | fixed | rejected
created: {{DATE}}
affects:                           # 每项 "REQ-NNN (条目号...)", 至少一个; 跨多个 REQ 就多写几项
  - REQ-NNN (FR-x, BR-y, AC-z)
summary: >-
  覆盖这行: 改了什么, 一句话
---

# {{ID}}: {{TITLE}}

<!-- 这是**变更 CR**: 改一个已有 implemented REQ 的行为. 全新业务用 cr-new.md (kind: charter).
     本文回答 "为什么原需求不再成立, 具体改哪几条条目". -->

## 1. 变更动机

<!-- 什么触发了这次变更: 业务调整, 合规要求, 技术约束, 线上问题. 附证据出处
     (工单, 事故记录, 产品确认的日期). 动机写的是 "为什么原需求不再成立",
     不是重复变更内容. 面向半年后第一次读到的人. -->

## 2. 变更内容

<!-- 精确的 delta, 按受影响条目逐条写 "现行 -> 变更后". 读者只看这一节就应能准确
     执行对 REQ 的修改, 所以:
     - 新增条目 "现行" 写 "无", 条目号续编该 REQ 当前最大号, 并在条目栏标 "(新增)";
       新增 FR 必须带出新增 AC.
     - 删除条目 "变更后" 写 "删除", 并给出 **原因** 与 **存量处置** (借 OpenSpec REMOVED
       的要求: 没有迁移说明的删除会让在途数据无处安放).
     - FR / BR 变化时, 受影响的 AC 也逐条列进来, 保持 REQ 第 9 节与第 4 / 5 节同步.
     - 状态机 / 流程图变化时, 贴变更后的 mermaid 片段并同步迁移表.
     - 不变的部分单独一句说明 "不变的是什么", 防止读者以为整段都重写了. -->

| 条目 | 现行 | 变更后 |
|---|---|---|
| FR-x | | |
| AC-y | | |
| FR-n (新增) | 无 | |
| AC-m (新增, FR-n) | 无 | |

**不变的部分**:

## 3. 影响分析

<!-- 逐项过, 不适用写 "无":
     - 同一 REQ 内的其他条目
     - 其他 REQ
     - API 合同与已有调用方
     - 数据与迁移 (存量要不要回填; 有没有预期内的中间状态)
     - 在途开发与已上线行为 (是否需要兼容期; 新旧代码并跑的窗口) -->

## 4. 替代方案

<!-- 考虑过但没采纳的做法及原因. 没有就写 "无". 这一节防止半年后有人重新提出
     已被否掉的方案. review 里否决的方案也记在这里 (带 review 编号, 如 "D-3"). -->

## 5. 落实

<!-- 由 /implement-cr 逐步填写. CR 置 fixed 的条件: 三次 review 都 fixed, REQ 正文已更新到
     新结论, REQ 变更记录已追加指回本 CR 的行, 代码与 REQ 更新在同一分支. -->

- 实施 spec: work/{{ID}}-{{SLUG}}/spec.md
- review: docs - / spec - / impl -   <!-- 各自的状态与结论, 如 "docs fixed (原 BLOCK, 3 条已修)" -->
- 实现提交:
- REQ 更新提交:
- 备注:

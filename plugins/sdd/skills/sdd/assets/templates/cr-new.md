---
id: {{ID}}
kind: charter                      # 全新业务立项; 改已有 REQ 的行为用 cr.md (kind: change)
status: to fix                     # to fix | fixing | fixed | rejected
created: {{DATE}}
affects:                           # 本次新建的 REQ; 分期实现时把 (全文) 换成条目号
  - REQ-NNN (全文)
summary: >-
  覆盖这行: 做一个什么业务, 一句话
---

# {{ID}}: {{TITLE}}

<!-- 这是**新增业务**的立项 CR: 从无到有做一个业务. 目标形态写在新建的 REQ 里, 本文回答
     "为什么现在做, 本期交付到哪, 对现有系统有什么影响, 否决过什么方案".
     与变更 CR (cr.md) 的差别只在第 1-3 节: 那边写 "现行 -> 变更后" 的 delta, 这边没有 "现行".
     判断用哪个: 目标业务已经有现行 REQ 就用 cr.md, 没有就用本模板.
     为什么全新业务也要 CR: 它是这次工作的单元 -- spec 与三次 review 都挂在 CR 目录下,
     实施状态与落实记录也在这里. 这不是变更管控 (draft REQ 本来就随便改), 是工作单元. -->

## 1. 立项动机

<!-- 为什么现在做这个业务, 不做会怎样. 触发来源 (产品决定, 合规要求, 客户需求, 线上问题)
     给出处与日期.
     与 REQ 第 1 节的分工: REQ 写 "这个业务解决什么问题" (只要业务还在就长期成立),
     这里写 "为什么是现在, 为什么值得这个成本" (一次性的立项理由). 不重复 REQ 正文. -->

## 2. 交付范围

<!-- 本期把 REQ 做到什么程度.
     **默认 REQ 描述的就是本期形态**, 这一节写 "REQ-NNN 全文" 即可.
     想好了但本期不做的能力, 写进 REQ 第 2 节 "非目标" (叙述, 不编号), 或者干脆不写, 等下次
     变更 CR 再加编号条目 -- 不要在 REQ 里写了编号条目却不实现: implemented 的 REQ 里
     未带标注的条目即已实现, 写了不做就得标 "(未实现)", 而 implemented + 未实现标注是矛盾的
     (validate 会报错).
     只有确实要分批合并实现时才逐条列, 并写明未交付条目的标注方式与后续 CR. -->

REQ-NNN 全文.

<!-- 分批时改用下表, 并在 REQ 对应条目行内标注 "(未实现, 见 CR-NNN)":

| 本期交付 | 条目 |
|---|---|
| 功能需求 | FR-1 ~ FR-x |
| 业务规则 | BR-1 ~ BR-x |
| 验收标准 | AC-1 ~ AC-x |

**本期不做**: FR-y (见 CR-NNN) -->

## 3. 对现有系统的影响

<!-- 新业务最容易漏的一节 -- 写新功能的人往往只盯着新代码. 逐项过, 不适用写 "无":
     - **依赖的现有能力**: 哪些 REQ 的行为是本业务成立的前提 (写编号); 读哪些表, 调哪些服务
     - **改变现有行为吗**: 新 worker / 队列 / 定时任务会不会与现有的争用同一资源或同一把锁;
       往共享表加列或加约束会不会让现有写入路径失败; 新 API 路径会不会撞现有路由;
       新增的 provider 调用会不会共用现有的幂等键空间
     - **API 合同**: 新增端点与 DTO
     - **数据与迁移**: 新表 / 新列; 存量要不要回填; 迁移能否独立于代码合并
     - **开关与灰度**: 默认开还是关, 怎么灰度, 怎么回退; 出问题时关掉它能不能让系统回到今天的状态 -->

## 4. 替代方案

<!-- 考虑过但没采纳的做法及原因. 新业务的选型自由度比变更大得多, 这一节比变更 CR 更重要:
     半年后有人问 "为什么不直接用现成的 X", 答案在这里. 没有就写 "无". -->

## 5. 落实

<!-- 由 /implement-cr 逐步填写. CR 置 fixed 的条件: 三次 review 都 fixed; REQ 与实现核对到位
     并置 implemented; REQ 变更记录首行写明由本 CR 立项; 代码与 REQ 更新在同一分支. -->

- 实施 spec: work/{{ID}}-{{SLUG}}/spec.md
- review: docs - / spec - / impl -   <!-- 各自的状态与结论 -->
- 实现提交:
- REQ 更新提交:
- 备注:
